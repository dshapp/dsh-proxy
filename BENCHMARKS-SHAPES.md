# 方案对比 — 实测（同一 bridge、同一 harness、M4 Pro 回环）

四种 proxy 形态，客户端协议一律是 HTTPS / WSS。

| 形态 | proxy 每请求做的事 |
|---|---|
| A 老方案 | 裸 TCP 盲转发；Noise_IK 端到端，proxy 不碰 |
| B 新方案·逐请求 HTTP | TLS 终止 + HTTP 解析/再发起 + 连接池 + Noise_IK + mux |
| C 新方案·WSS 字节拼接 | TLS 终止 + copy_bidirectional + Noise_IK + mux |
| D C 再去掉内层 Noise_IK | TLS 终止 + copy_bidirectional + mux |

## 结果

| 场景 | 指标 | A 老 | B 逐请求 HTTP | C WSS 拼接 | D 去内层 Noise |
|---|---|---:|---:|---:|---:|
| 4 B，1 连接 | rps | 7 567 | 14 799 | 18 507 | **19 008** |
| | p99 | 1.739 ms | 0.120 ms | 0.106 ms | 0.113 ms |
| | CPU | 0:00.09 | 0:00.13 | 0:00.09 | 0:00.09 |
| 4 B，32 连接 | rps | 88 418 | 59 665 | 106 677 | **109 224** |
| | p50 / p99 | 0.329 / 0.869 | 0.408 / 1.339 | 0.291 / 0.672 | 0.280 / 0.747 |
| | CPU | **0:00.58** | 0:02.47 | 0:01.04 | 0:00.93 |
| | RSS | **3 920 KB** | 9 120 KB | 6 688 KB | 6 128 KB |
| 256 KiB ×200 | MB/s | **231.9** | 177.4 | 167.0 | (未测) |
| 1 MiB ×64 conc 8 | MB/s | **555.6** | 548.4 | 462.6 | (未测) |
| | RSS | **5 360 KB** | 34 512 KB | 10 560 KB | (未测) |

## 结论

**1. 性能提升的 93% 来自「不再逐请求解析并再发起 HTTP」，不是来自「不加密」。**
32 并发从 B 换到 C：59 665 → 106 677 rps，CPU 2.47 → 1.04 s。再从 C 换到 D
（proxy 完全不做应用层加密）：只多 2.4 % rps、省 11 % CPU。因为小 payload 的
瓶颈是每请求的固定开销（TLS 记录 + 两遍 HTTP 编解码 + HeaderMap clone + 连接
池锁 + 6 次异步任务跳转），不是 AEAD 吞吐。

**2. C 在 32 并发下比老方案还快 21 %**，且仍然只暴露 HTTPS/WSS。代价是 CPU 为
老方案的 1.8 倍（每字节多一遍 TLS AEAD 和一次拷贝），内存 1.7 倍。

**3. 大块吞吐 C 低于 A/B**（462 vs 555 MB/s），但 proxy CPU 几乎相同
（0.35 vs 0.32 s），说明差距在压测客户端的朴素分帧（每帧一次分配 + 两次系统
调用，无流水线），不是 proxy 的上限。

## D 形态的已知缺陷

`DSH_TUNNEL_PLAIN=1` 是实验开关，**16 KiB 以上会挂死**：去掉 NoiseStream 后，
`MuxStreamIo` 的 32 KiB 合并缓冲区失去了每条 Noise 消息一次的强制 flush，暴露
出与之前那个「完整消息滞留缓冲区」同类的问题。所以上表 D 列只有小 payload 的
数字是可信的，大块一格留空，没有估算填充。要把 D 做成产品形态，必须先修这个，
并且要改 dsh-mobile-bridge 的协议（用一条经 Noise_XX 链路背书的控制帧代替
每条隧道的 IK 握手）。
